name: Continue on error with secrets
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: may-fail
        continue-on-error: true
      - run: curl -H "Authorization: ${{ secrets.TOKEN }}" https://api.example.com
