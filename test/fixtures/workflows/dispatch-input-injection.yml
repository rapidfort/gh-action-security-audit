name: Manual Deploy
on:
  workflow_dispatch:
    inputs:
      target:
        description: Deploy target
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploying to ${{ inputs.target }}"
