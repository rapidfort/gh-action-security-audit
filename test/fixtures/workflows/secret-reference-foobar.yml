name: Deploy Extended
on: push

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Deploying"
        env:
          API_KEY: ${{ secrets.FOOBAR }}
