name: Post-Build
on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "analyzing"
